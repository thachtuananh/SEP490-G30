version: "3.9"

services:

  web:

    build: .

    ports:

      - "8080:8080"

    network_mode: host

    environment:

      - SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/doanan

      - SPRING_DATASOURCE_USERNAME=postgres

      - SPRING_DATASOURCE_PASSWORD=123
